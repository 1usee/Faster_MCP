# 运行依赖（与 pyproject.toml 保持一致）
# 只装了这一个 SDK，就能起一个标准 MCP 服务端。
#
# 版本策略：锁在 1.x。官方 SDK v2 是破坏性重构，升级需要改 server.py 的挂载方式。
# 注意：下面的范围只是"避免冲突"，不能保证可复现。
# 如果你在某个具体版本上跑通了，请把它改成精确版本（如 mcp==1.9.4）——
# 未验证过的宽范围，等于把风险留给下一个人。
mcp>=1.2,<2
pydantic>=2.0

# 开发/测试依赖（可选）
# pytest>=7.4
